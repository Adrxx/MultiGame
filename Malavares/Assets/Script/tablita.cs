using UnityEngine;
using System.Collections;

public class tablita : MonoBehaviour {
    public float escalar = .0007f;

	void Start () {
	
	}
	
	void Update () {
        //if (Input.GetKey("right")) this.transform.rotation = new Vector2(this.transform.rotation.x + escalar, this.transform.rotation.y);
	}
}
